# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets (France Travail API creds etc.)
jobboard/.env
.env

# Job board — static SEO pages (regenerated every run by render_pages.py,
# deployed straight from the Pages artifact — not tracked)
jobboard/site/offre/
jobboard/site/emploi/
jobboard/site/sitemap*.xml
jobboard/site/robots.txt

# Job board — regenerable artefacts (kept OUT of git; CI rebuilds them)
jobboard/data/cache/
jobboard/data/wttj_paca.raw.json
jobboard/jobs.json
jobboard/jobs.csv
jobboard/jobs.paca.json
jobboard/jobs.paca.csv
jobboard/data/ats_jobs.csv

# Nested project with its own repo — not part of this one
sonarfeed/
